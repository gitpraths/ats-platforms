{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npm run build && cp server.js dist/server.js"
  },
  "deploy": {
    "startCommand": "node dist/server.js"
  }
}
