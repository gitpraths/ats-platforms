[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]
cacheDirectories = ["/root/.npm", "node_modules"]

[phases.build]
cmds = ["npm run build"]
cacheDirectories = ["node_modules/.vite"]

[start]
cmd = "node server.js"
